import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.tutorsessions.app',
  appName: 'Tutor Session Tracker',
  webDir: 'dist'
};

export default config;
